package Negocio; 

import java.util.UUID;

public class NewMain { 
    public static void main(String[] args) { 
        GameSession gs = new GameSession();
        /**
        UUID uuid = UUID.randomUUID(); 
        gs.addPlayer(new Player(uuid, "prueba"));
        uuid = UUID.randomUUID();
        gs.addPlayer(new Player(uuid, "prueba"));
        uuid = UUID.randomUUID();
        gs.addPlayer(new Player(uuid, "prueba"));
        uuid = UUID.randomUUID();
        gs.addPlayer(new Player(uuid, "prueba"));
        
        gs.showPlayers();
        * */
    } 
}
